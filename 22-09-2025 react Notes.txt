import React,{useState} from 'react'
const App=()=>{
  const[count,setCount]=useState(0)
  const[count2,setCount2]=useState(0)
    const valueUpdaater=()=>{
      setCount(count+1)
    
  }
  const valueUpdater2=()=>{
      setCount(count-1)
  }
    return(
    <div>
    <h3>React</h3>
    <p>{count}</p>
    <button onClick={valueUpdaater}>Increment</button>
    <button onClick={valueUpdater2}>Decrementation</button>
    </div>);
  
}
export default App;